const mongoose = require("mongoose");
const db = require("../models");

const userSeed = {
    username: 'admin',
  }

  const runSeeder = async () => {
    try {
      
    } catch(err) {
      throw new err
    }
    process.exit()
  }
  
  runSeeder()
  